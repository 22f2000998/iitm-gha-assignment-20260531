name: assignment-action

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: 23f2000421@ds.study.iitm.ac.in
        run: echo "Hello, world!"
